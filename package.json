{
  "name": "worldcup-betting",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "setup": "npm install --prefix server && npm install --prefix client && npm run db:push --prefix server",
    "seed": "npm run seed --prefix server",
    "vercel-build": "(cd server && npm install && npx prisma generate && npx prisma db push && node src/seed.js) && (cd client && npm install && npm run build)"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
